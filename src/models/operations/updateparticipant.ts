/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as components from "../components/index.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export const UpdateParticipantServerList = [
  "https://api.twilio.com",
] as const;

/**
 * The HTTP method we should use to call `hold_url`. Can be: `GET` or `POST` and the default is `GET`.
 */
export const HoldMethod = {
  Get: "GET",
  Post: "POST",
} as const;
/**
 * The HTTP method we should use to call `hold_url`. Can be: `GET` or `POST` and the default is `GET`.
 */
export type HoldMethod = ClosedEnum<typeof HoldMethod>;

/**
 * The HTTP method we should use to call `announce_url`. Can be: `GET` or `POST` and defaults to `POST`.
 */
export const UpdateParticipantAnnounceMethod = {
  Get: "GET",
  Post: "POST",
} as const;
/**
 * The HTTP method we should use to call `announce_url`. Can be: `GET` or `POST` and defaults to `POST`.
 */
export type UpdateParticipantAnnounceMethod = ClosedEnum<
  typeof UpdateParticipantAnnounceMethod
>;

/**
 * The HTTP method we should use to call `wait_url`. Can be `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so that we can cache the file.
 */
export const WaitMethod = {
  Get: "GET",
  Post: "POST",
} as const;
/**
 * The HTTP method we should use to call `wait_url`. Can be `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so that we can cache the file.
 */
export type WaitMethod = ClosedEnum<typeof WaitMethod>;

export type UpdateParticipantUpdateParticipantRequest = {
  /**
   * Whether the participant should be muted. Can be `true` or `false`. `true` will mute the participant, and `false` will un-mute them. Anything value other than `true` or `false` is interpreted as `false`.
   */
  muted?: boolean | undefined;
  /**
   * Whether the participant should be on hold. Can be: `true` or `false`. `true` puts the participant on hold, and `false` lets them rejoin the conference.
   */
  hold?: boolean | undefined;
  /**
   * The URL we call using the `hold_method` for music that plays when the participant is on hold. The URL may return an MP3 file, a WAV file, or a TwiML document that contains `<Play>`, `<Say>`, `<Pause>`, or `<Redirect>` verbs.
   */
  holdUrl?: string | undefined;
  /**
   * The HTTP method we should use to call `hold_url`. Can be: `GET` or `POST` and the default is `GET`.
   */
  holdMethod?: HoldMethod | undefined;
  /**
   * The URL we call using the `announce_method` for an announcement to the participant. The URL may return an MP3 file, a WAV file, or a TwiML document that contains `<Play>`, `<Say>`, `<Pause>`, or `<Redirect>` verbs.
   */
  announceUrl?: string | undefined;
  /**
   * The HTTP method we should use to call `announce_url`. Can be: `GET` or `POST` and defaults to `POST`.
   */
  announceMethod?: UpdateParticipantAnnounceMethod | undefined;
  /**
   * The URL we call using the `wait_method` for the music to play while participants are waiting for the conference to start. The URL may return an MP3 file, a WAV file, or a TwiML document that contains `<Play>`, `<Say>`, `<Pause>`, or `<Redirect>` verbs. The default value is the URL of our standard hold music. [Learn more about hold music](https://www.twilio.com/labs/twimlets/holdmusic).
   */
  waitUrl?: string | undefined;
  /**
   * The HTTP method we should use to call `wait_url`. Can be `GET` or `POST` and the default is `POST`. When using a static audio file, this should be `GET` so that we can cache the file.
   */
  waitMethod?: WaitMethod | undefined;
  /**
   * Whether to play a notification beep to the conference when the participant exits. Can be: `true` or `false`.
   */
  beepOnExit?: boolean | undefined;
  /**
   * Whether to end the conference when the participant leaves. Can be: `true` or `false` and defaults to `false`.
   */
  endConferenceOnExit?: boolean | undefined;
  /**
   * Whether the participant is coaching another call. Can be: `true` or `false`. If not present, defaults to `false` unless `call_sid_to_coach` is defined. If `true`, `call_sid_to_coach` must be defined.
   */
  coaching?: boolean | undefined;
  /**
   * The SID of the participant who is being `coached`. The participant being coached is the only participant who can hear the participant who is `coaching`.
   */
  callSidToCoach?: string | undefined;
};

export type UpdateParticipantRequest = {
  /**
   * The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Participant resources to update.
   */
  accountSid: string;
  /**
   * The SID of the conference with the participant to update.
   */
  conferenceSid: string;
  /**
   * The [Call](https://www.twilio.com/docs/voice/api/call-resource) SID or label of the participant to update. Non URL safe characters in a label must be percent encoded, for example, a space character is represented as %20.
   */
  callSid: string;
  requestBody?: UpdateParticipantUpdateParticipantRequest | undefined;
};

export type UpdateParticipantResponse = {
  headers: { [k: string]: Array<string> };
  result: components.ApiV2010AccountConferenceParticipant;
};

/** @internal */
export const HoldMethod$inboundSchema: z.ZodNativeEnum<typeof HoldMethod> = z
  .nativeEnum(HoldMethod);

/** @internal */
export const HoldMethod$outboundSchema: z.ZodNativeEnum<typeof HoldMethod> =
  HoldMethod$inboundSchema;

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace HoldMethod$ {
  /** @deprecated use `HoldMethod$inboundSchema` instead. */
  export const inboundSchema = HoldMethod$inboundSchema;
  /** @deprecated use `HoldMethod$outboundSchema` instead. */
  export const outboundSchema = HoldMethod$outboundSchema;
}

/** @internal */
export const UpdateParticipantAnnounceMethod$inboundSchema: z.ZodNativeEnum<
  typeof UpdateParticipantAnnounceMethod
> = z.nativeEnum(UpdateParticipantAnnounceMethod);

/** @internal */
export const UpdateParticipantAnnounceMethod$outboundSchema: z.ZodNativeEnum<
  typeof UpdateParticipantAnnounceMethod
> = UpdateParticipantAnnounceMethod$inboundSchema;

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace UpdateParticipantAnnounceMethod$ {
  /** @deprecated use `UpdateParticipantAnnounceMethod$inboundSchema` instead. */
  export const inboundSchema = UpdateParticipantAnnounceMethod$inboundSchema;
  /** @deprecated use `UpdateParticipantAnnounceMethod$outboundSchema` instead. */
  export const outboundSchema = UpdateParticipantAnnounceMethod$outboundSchema;
}

/** @internal */
export const WaitMethod$inboundSchema: z.ZodNativeEnum<typeof WaitMethod> = z
  .nativeEnum(WaitMethod);

/** @internal */
export const WaitMethod$outboundSchema: z.ZodNativeEnum<typeof WaitMethod> =
  WaitMethod$inboundSchema;

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace WaitMethod$ {
  /** @deprecated use `WaitMethod$inboundSchema` instead. */
  export const inboundSchema = WaitMethod$inboundSchema;
  /** @deprecated use `WaitMethod$outboundSchema` instead. */
  export const outboundSchema = WaitMethod$outboundSchema;
}

/** @internal */
export const UpdateParticipantUpdateParticipantRequest$inboundSchema: z.ZodType<
  UpdateParticipantUpdateParticipantRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  Muted: z.boolean().optional(),
  Hold: z.boolean().optional(),
  HoldUrl: z.string().optional(),
  HoldMethod: HoldMethod$inboundSchema.optional(),
  AnnounceUrl: z.string().optional(),
  AnnounceMethod: UpdateParticipantAnnounceMethod$inboundSchema.optional(),
  WaitUrl: z.string().optional(),
  WaitMethod: WaitMethod$inboundSchema.optional(),
  BeepOnExit: z.boolean().optional(),
  EndConferenceOnExit: z.boolean().optional(),
  Coaching: z.boolean().optional(),
  CallSidToCoach: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "Muted": "muted",
    "Hold": "hold",
    "HoldUrl": "holdUrl",
    "HoldMethod": "holdMethod",
    "AnnounceUrl": "announceUrl",
    "AnnounceMethod": "announceMethod",
    "WaitUrl": "waitUrl",
    "WaitMethod": "waitMethod",
    "BeepOnExit": "beepOnExit",
    "EndConferenceOnExit": "endConferenceOnExit",
    "Coaching": "coaching",
    "CallSidToCoach": "callSidToCoach",
  });
});

/** @internal */
export type UpdateParticipantUpdateParticipantRequest$Outbound = {
  Muted?: boolean | undefined;
  Hold?: boolean | undefined;
  HoldUrl?: string | undefined;
  HoldMethod?: string | undefined;
  AnnounceUrl?: string | undefined;
  AnnounceMethod?: string | undefined;
  WaitUrl?: string | undefined;
  WaitMethod?: string | undefined;
  BeepOnExit?: boolean | undefined;
  EndConferenceOnExit?: boolean | undefined;
  Coaching?: boolean | undefined;
  CallSidToCoach?: string | undefined;
};

/** @internal */
export const UpdateParticipantUpdateParticipantRequest$outboundSchema:
  z.ZodType<
    UpdateParticipantUpdateParticipantRequest$Outbound,
    z.ZodTypeDef,
    UpdateParticipantUpdateParticipantRequest
  > = z.object({
    muted: z.boolean().optional(),
    hold: z.boolean().optional(),
    holdUrl: z.string().optional(),
    holdMethod: HoldMethod$outboundSchema.optional(),
    announceUrl: z.string().optional(),
    announceMethod: UpdateParticipantAnnounceMethod$outboundSchema.optional(),
    waitUrl: z.string().optional(),
    waitMethod: WaitMethod$outboundSchema.optional(),
    beepOnExit: z.boolean().optional(),
    endConferenceOnExit: z.boolean().optional(),
    coaching: z.boolean().optional(),
    callSidToCoach: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      muted: "Muted",
      hold: "Hold",
      holdUrl: "HoldUrl",
      holdMethod: "HoldMethod",
      announceUrl: "AnnounceUrl",
      announceMethod: "AnnounceMethod",
      waitUrl: "WaitUrl",
      waitMethod: "WaitMethod",
      beepOnExit: "BeepOnExit",
      endConferenceOnExit: "EndConferenceOnExit",
      coaching: "Coaching",
      callSidToCoach: "CallSidToCoach",
    });
  });

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace UpdateParticipantUpdateParticipantRequest$ {
  /** @deprecated use `UpdateParticipantUpdateParticipantRequest$inboundSchema` instead. */
  export const inboundSchema =
    UpdateParticipantUpdateParticipantRequest$inboundSchema;
  /** @deprecated use `UpdateParticipantUpdateParticipantRequest$outboundSchema` instead. */
  export const outboundSchema =
    UpdateParticipantUpdateParticipantRequest$outboundSchema;
  /** @deprecated use `UpdateParticipantUpdateParticipantRequest$Outbound` instead. */
  export type Outbound = UpdateParticipantUpdateParticipantRequest$Outbound;
}

export function updateParticipantUpdateParticipantRequestToJSON(
  updateParticipantUpdateParticipantRequest:
    UpdateParticipantUpdateParticipantRequest,
): string {
  return JSON.stringify(
    UpdateParticipantUpdateParticipantRequest$outboundSchema.parse(
      updateParticipantUpdateParticipantRequest,
    ),
  );
}

export function updateParticipantUpdateParticipantRequestFromJSON(
  jsonString: string,
): SafeParseResult<
  UpdateParticipantUpdateParticipantRequest,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      UpdateParticipantUpdateParticipantRequest$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'UpdateParticipantUpdateParticipantRequest' from JSON`,
  );
}

/** @internal */
export const UpdateParticipantRequest$inboundSchema: z.ZodType<
  UpdateParticipantRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  AccountSid: z.string(),
  ConferenceSid: z.string(),
  CallSid: z.string(),
  RequestBody: z.lazy(() =>
    UpdateParticipantUpdateParticipantRequest$inboundSchema
  ).optional(),
}).transform((v) => {
  return remap$(v, {
    "AccountSid": "accountSid",
    "ConferenceSid": "conferenceSid",
    "CallSid": "callSid",
    "RequestBody": "requestBody",
  });
});

/** @internal */
export type UpdateParticipantRequest$Outbound = {
  AccountSid: string;
  ConferenceSid: string;
  CallSid: string;
  RequestBody?: UpdateParticipantUpdateParticipantRequest$Outbound | undefined;
};

/** @internal */
export const UpdateParticipantRequest$outboundSchema: z.ZodType<
  UpdateParticipantRequest$Outbound,
  z.ZodTypeDef,
  UpdateParticipantRequest
> = z.object({
  accountSid: z.string(),
  conferenceSid: z.string(),
  callSid: z.string(),
  requestBody: z.lazy(() =>
    UpdateParticipantUpdateParticipantRequest$outboundSchema
  ).optional(),
}).transform((v) => {
  return remap$(v, {
    accountSid: "AccountSid",
    conferenceSid: "ConferenceSid",
    callSid: "CallSid",
    requestBody: "RequestBody",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace UpdateParticipantRequest$ {
  /** @deprecated use `UpdateParticipantRequest$inboundSchema` instead. */
  export const inboundSchema = UpdateParticipantRequest$inboundSchema;
  /** @deprecated use `UpdateParticipantRequest$outboundSchema` instead. */
  export const outboundSchema = UpdateParticipantRequest$outboundSchema;
  /** @deprecated use `UpdateParticipantRequest$Outbound` instead. */
  export type Outbound = UpdateParticipantRequest$Outbound;
}

export function updateParticipantRequestToJSON(
  updateParticipantRequest: UpdateParticipantRequest,
): string {
  return JSON.stringify(
    UpdateParticipantRequest$outboundSchema.parse(updateParticipantRequest),
  );
}

export function updateParticipantRequestFromJSON(
  jsonString: string,
): SafeParseResult<UpdateParticipantRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateParticipantRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateParticipantRequest' from JSON`,
  );
}

/** @internal */
export const UpdateParticipantResponse$inboundSchema: z.ZodType<
  UpdateParticipantResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  Headers: z.record(z.array(z.string())),
  Result: components.ApiV2010AccountConferenceParticipant$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "Headers": "headers",
    "Result": "result",
  });
});

/** @internal */
export type UpdateParticipantResponse$Outbound = {
  Headers: { [k: string]: Array<string> };
  Result: components.ApiV2010AccountConferenceParticipant$Outbound;
};

/** @internal */
export const UpdateParticipantResponse$outboundSchema: z.ZodType<
  UpdateParticipantResponse$Outbound,
  z.ZodTypeDef,
  UpdateParticipantResponse
> = z.object({
  headers: z.record(z.array(z.string())),
  result: components.ApiV2010AccountConferenceParticipant$outboundSchema,
}).transform((v) => {
  return remap$(v, {
    headers: "Headers",
    result: "Result",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace UpdateParticipantResponse$ {
  /** @deprecated use `UpdateParticipantResponse$inboundSchema` instead. */
  export const inboundSchema = UpdateParticipantResponse$inboundSchema;
  /** @deprecated use `UpdateParticipantResponse$outboundSchema` instead. */
  export const outboundSchema = UpdateParticipantResponse$outboundSchema;
  /** @deprecated use `UpdateParticipantResponse$Outbound` instead. */
  export type Outbound = UpdateParticipantResponse$Outbound;
}

export function updateParticipantResponseToJSON(
  updateParticipantResponse: UpdateParticipantResponse,
): string {
  return JSON.stringify(
    UpdateParticipantResponse$outboundSchema.parse(updateParticipantResponse),
  );
}

export function updateParticipantResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateParticipantResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateParticipantResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateParticipantResponse' from JSON`,
  );
}
