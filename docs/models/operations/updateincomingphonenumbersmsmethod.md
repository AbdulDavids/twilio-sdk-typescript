# UpdateIncomingPhoneNumberSmsMethod

The HTTP method that we should use to call `sms_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { UpdateIncomingPhoneNumberSmsMethod } from "twilio-sdk/models/operations";

let value: UpdateIncomingPhoneNumberSmsMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```