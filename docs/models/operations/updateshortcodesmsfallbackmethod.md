# UpdateShortCodeSmsFallbackMethod

The HTTP method that we should use to call the `sms_fallback_url`. Can be: `GET` or `POST`.

## Example Usage

```typescript
import { UpdateShortCodeSmsFallbackMethod } from "twilio-sdk/models/operations";

let value: UpdateShortCodeSmsFallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```