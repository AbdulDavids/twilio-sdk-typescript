# SmsMethod

The HTTP method we should use to call `sms_url`. Can be: `GET` or `POST`.

## Example Usage

```typescript
import { SmsMethod } from "twilio-sdk/models/operations";

let value: SmsMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```