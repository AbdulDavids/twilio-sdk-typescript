# CreateIncomingPhoneNumberVoiceMethod

The HTTP method that we should use to call `voice_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberVoiceMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberVoiceMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```