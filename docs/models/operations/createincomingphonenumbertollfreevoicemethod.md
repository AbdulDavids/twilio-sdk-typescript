# CreateIncomingPhoneNumberTollFreeVoiceMethod

The HTTP method that we should use to call `voice_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberTollFreeVoiceMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberTollFreeVoiceMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```