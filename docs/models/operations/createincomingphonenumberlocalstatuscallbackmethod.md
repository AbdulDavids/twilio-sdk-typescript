# CreateIncomingPhoneNumberLocalStatusCallbackMethod

The HTTP method we should use to call `status_callback`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberLocalStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberLocalStatusCallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```