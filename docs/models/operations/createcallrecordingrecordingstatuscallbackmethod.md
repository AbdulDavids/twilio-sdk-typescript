# CreateCallRecordingRecordingStatusCallbackMethod

The HTTP method we should use to call `recording_status_callback`. Can be: `GET` or `POST` and the default is `POST`.

## Example Usage

```typescript
import { CreateCallRecordingRecordingStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateCallRecordingRecordingStatusCallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```