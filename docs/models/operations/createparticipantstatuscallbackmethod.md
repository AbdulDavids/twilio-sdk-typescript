# CreateParticipantStatusCallbackMethod

The HTTP method we should use to call `status_callback`. Can be: `GET` and `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateParticipantStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateParticipantStatusCallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```