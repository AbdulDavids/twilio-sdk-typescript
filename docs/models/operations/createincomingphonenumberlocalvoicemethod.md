# CreateIncomingPhoneNumberLocalVoiceMethod

The HTTP method that we should use to call `voice_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberLocalVoiceMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberLocalVoiceMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```