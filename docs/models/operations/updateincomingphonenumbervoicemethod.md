# UpdateIncomingPhoneNumberVoiceMethod

The HTTP method that we should use to call `voice_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { UpdateIncomingPhoneNumberVoiceMethod } from "twilio-sdk/models/operations";

let value: UpdateIncomingPhoneNumberVoiceMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```