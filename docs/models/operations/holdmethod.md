# HoldMethod

The HTTP method we should use to call `hold_url`. Can be: `GET` or `POST` and the default is `GET`.

## Example Usage

```typescript
import { HoldMethod } from "twilio-sdk/models/operations";

let value: HoldMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```