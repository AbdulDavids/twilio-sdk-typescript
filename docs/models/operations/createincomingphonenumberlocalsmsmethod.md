# CreateIncomingPhoneNumberLocalSmsMethod

The HTTP method that we should use to call `sms_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberLocalSmsMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberLocalSmsMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```