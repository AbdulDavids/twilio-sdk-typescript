# UpdateParticipantAnnounceMethod

The HTTP method we should use to call `announce_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { UpdateParticipantAnnounceMethod } from "twilio-sdk/models/operations";

let value: UpdateParticipantAnnounceMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```