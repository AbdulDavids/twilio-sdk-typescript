# CreateIncomingPhoneNumberTollFreeSmsMethod

The HTTP method that we should use to call `sms_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberTollFreeSmsMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberTollFreeSmsMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```