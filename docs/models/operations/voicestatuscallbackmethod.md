# VoiceStatusCallbackMethod

The HTTP method we should use to call `voice_status_callback_url`. Can be: `GET` or `POST`.

## Example Usage

```typescript
import { VoiceStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: VoiceStatusCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```