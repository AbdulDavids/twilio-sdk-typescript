# CreateStreamStatusCallbackMethod

The HTTP method Twilio uses when sending `status_callback` requests. Possible values are `GET` and `POST`. Default is `POST`.

## Example Usage

```typescript
import { CreateStreamStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateStreamStatusCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```