# CreateParticipantRecordingStatusCallbackMethod

The HTTP method we should use when we call `recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateParticipantRecordingStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateParticipantRecordingStatusCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```