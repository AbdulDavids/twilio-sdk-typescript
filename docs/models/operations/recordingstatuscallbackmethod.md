# RecordingStatusCallbackMethod

The HTTP method we should use when calling the `recording_status_callback` URL. Can be: `GET` or `POST` and the default is `POST`.

## Example Usage

```typescript
import { RecordingStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: RecordingStatusCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```