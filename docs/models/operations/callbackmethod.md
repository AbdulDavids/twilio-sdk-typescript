# CallbackMethod

The HTTP method we should use to call `callback_url`. Can be: `GET` or `POST` and the default is `POST`.

## Example Usage

```typescript
import { CallbackMethod } from "twilio-sdk/models/operations";

let value: CallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```