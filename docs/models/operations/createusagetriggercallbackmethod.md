# CreateUsageTriggerCallbackMethod

The HTTP method we should use to call `callback_url`. Can be: `GET` or `POST` and the default is `POST`.

## Example Usage

```typescript
import { CreateUsageTriggerCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateUsageTriggerCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```