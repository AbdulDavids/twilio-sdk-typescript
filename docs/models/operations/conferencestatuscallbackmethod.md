# ConferenceStatusCallbackMethod

The HTTP method we should use to call `conference_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { ConferenceStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: ConferenceStatusCallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```