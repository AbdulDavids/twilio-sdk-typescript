# AnnounceMethod

The HTTP method used to call `announce_url`. Can be: `GET` or `POST` and the default is `POST`

## Example Usage

```typescript
import { AnnounceMethod } from "twilio-sdk/models/operations";

let value: AnnounceMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```