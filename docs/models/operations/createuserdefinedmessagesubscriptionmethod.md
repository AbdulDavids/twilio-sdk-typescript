# CreateUserDefinedMessageSubscriptionMethod

The HTTP method Twilio will use when requesting the above `Url`. Either `GET` or `POST`. Default is `POST`.

## Example Usage

```typescript
import { CreateUserDefinedMessageSubscriptionMethod } from "twilio-sdk/models/operations";

let value: CreateUserDefinedMessageSubscriptionMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```