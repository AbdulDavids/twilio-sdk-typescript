# CreateValidationRequestStatusCallbackMethod

The HTTP method we should use to call `status_callback`. Can be: `GET` or `POST`, and the default is `POST`.

## Example Usage

```typescript
import { CreateValidationRequestStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: CreateValidationRequestStatusCallbackMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```