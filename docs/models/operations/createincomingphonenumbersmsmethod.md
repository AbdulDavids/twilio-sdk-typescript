# CreateIncomingPhoneNumberSmsMethod

The HTTP method that we should use to call `sms_url`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { CreateIncomingPhoneNumberSmsMethod } from "twilio-sdk/models/operations";

let value: CreateIncomingPhoneNumberSmsMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```