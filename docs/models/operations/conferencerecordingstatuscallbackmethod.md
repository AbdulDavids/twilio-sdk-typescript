# ConferenceRecordingStatusCallbackMethod

The HTTP method we should use to call `conference_recording_status_callback`. Can be: `GET` or `POST` and defaults to `POST`.

## Example Usage

```typescript
import { ConferenceRecordingStatusCallbackMethod } from "twilio-sdk/models/operations";

let value: ConferenceRecordingStatusCallbackMethod = "POST";
```

## Values

```typescript
"GET" | "POST"
```