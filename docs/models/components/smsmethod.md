# SmsMethod

The HTTP method we use to call `sms_url`. Can be: `GET` or `POST`.

## Example Usage

```typescript
import { SmsMethod } from "twilio-sdk/models/components";

let value: SmsMethod = "GET";
```

## Values

```typescript
"GET" | "POST"
```